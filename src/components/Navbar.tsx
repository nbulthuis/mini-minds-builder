import * as React from 'react'

const Navbar: React.FunctionComponent<{user?: string}> = ({user: "Guest"}) => {
  const [timer, setTimer] = useState(0);
  
  return(
    <>
    </>
  )
}

export default Navbar